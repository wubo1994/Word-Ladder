1. Put all the source files and that makefile under same folder

2. Open that makefile and change the path after BOOST_HOME to the path of the boost library on your machine

3. Run "make" command through command line